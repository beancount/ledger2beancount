language: python
python:
  - "3.6"
env:
  - BEANCOUNT="beancount>=2.0"
  - BEANCOUNT="hg+https://bitbucket.org/blais/beancount/"
addons:
  apt:
    packages:
      - cpanminus
      - libcarp-assert-perl
      - libdate-calc-perl
      - libexperimental-perl
      - libfile-basedir-perl
      - libgetopt-long-descriptive-perl
      - libyaml-libyaml-perl
      - ledger
matrix:
  allow_failures:
  - env: BEANCOUNT="beancount>=2.0"
install:
  - sudo cpanm Config::Onion
  - sudo cpanm DateTime::Format::Strptime
  - pip install $BEANCOUNT
script:
  - make test
